<Window x:Class="test.wCoinsEx"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:test"
        mc:Ignorable="d"
        Height="300" Width="300" WindowStyle="ToolWindow" Topmost="True" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button FontSize="36" Content="-" Grid.Column="0" Click="Down_Click"/>
            <Label Content="1" Grid.Column="1" x:Name="lbCount" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="24" FontWeight="Bold"/>
            <Button Content="+" Grid.Column="2" FontSize="36" Click="Up_Click"/>
        </Grid>
        
        <Button Content="Внести" Margin="20" Grid.Row="1" Click="Button_Click"/>
    </Grid>
</Window>
